{
  "build": {
    "builder": "NIXPACKS",
    "commands": ["npm ci", "npm run build"]
  },
  "deploy": {
    "startCommand": "npx serve -s dist/ng-habado",
    "healthcheckPath": "/",
    "staticPublishPath": "dist/ng-habado"
  }
}
